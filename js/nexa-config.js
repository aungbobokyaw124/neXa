/* neXa runtime configuration — public values only. Never place secrets here. */
window.NEXA_CONFIG = Object.freeze({
  brand: 'neXa',
  tagline: 'Build Faster. Create Smarter.',
  apiBaseUrl: '',
  supabaseUrl: '',
  supabasePublishableKey: ''
});
